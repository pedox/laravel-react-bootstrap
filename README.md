# Laravel UI with React SPA

Minimal laravel ui with

- React (SPA)
- Bootstrap
- Reactstrap
- Formik

Just put all resources pages and go on.

use `react.blade.php` as default view

use `$data` for storing app context

Detailed Documentation soon...
